import type { Metadata } from "next";
import { AdminPreview } from "@/features/admin/admin-preview";

export const metadata: Metadata = { title: "Admin interface preview" };
export default AdminPreview;
